name: iocheck

services:
  postgres:
    image: postgres:16-alpine
    container_name: iocheck-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-iocheck}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-iocheck}
      POSTGRES_DB: ${POSTGRES_DB:-iocheck}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
