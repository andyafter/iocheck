iocheck has been installed.

Service URL in Minikube:
  minikube service {{ include "iocheck.fullname" . }} --url

Prometheus URL in Minikube:
  minikube service {{ include "iocheck.prometheusName" . }} --url

Grafana URL in Minikube:
  minikube service {{ include "iocheck.grafanaName" . }} --url

Grafana login:
  username: {{ .Values.grafana.admin.user }}
  password: {{ .Values.grafana.admin.password }}

Change resources and storage in:
  helm/iocheck/values.yaml
